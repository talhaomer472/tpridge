% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators.R
\name{tpr_eff_df}
\alias{tpr_eff_df}
\title{Effective degrees of freedom of the ridge operator}
\usage{
tpr_eff_df(x, lambda, standardize = TRUE)
}
\arguments{
\item{x}{Either a numeric matrix of regressors, or a vector of
eigenvalues of the cross-product matrix.}

\item{lambda}{Non-negative penalty.}

\item{standardize}{Logical. If `x` is a matrix, standardize its
columns before forming the cross-product. Default `TRUE`.}
}
\value{
A single number, the effective degrees of freedom.
}
\description{
The quantity \eqn{\mathrm{df}(\lambda)=\sum_j e_j/(e_j+\lambda)},
where \eqn{e_j} are the eigenvalues of \eqn{X'X}. It falls from
\eqn{p} at \eqn{\lambda=0} towards zero as the penalty grows, and is
comparable across sample sizes and designs in a way that \eqn{\lambda}
itself is not.
}
\examples{
set.seed(1)
X <- matrix(rnorm(300), ncol = 3)
tpr_eff_df(X, lambda = 0)
tpr_eff_df(X, lambda = 1e6)
}
