% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{tpr_mcs}
\alias{tpr_mcs}
\title{Model confidence set}
\usage{
tpr_mcs(loss, alpha = 0.1, B = 5000L, statistic = "Tmax")
}
\arguments{
\item{loss}{Matrix of losses, one column per model.}

\item{alpha}{Size of the test. Default `0.10`.}

\item{B}{Bootstrap replications. Default `5000`.}

\item{statistic}{Either `"Tmax"` or `"TR"`.}
}
\value{
A `data.table`, or `NULL` if the procedure fails.
}
\description{
A thin wrapper around `MCS::MCSprocedure` that accepts a matrix of
losses and returns a tidy table, extracting the result slot across
the versions of that package.
}
\examples{
\donttest{
set.seed(1)
L <- cbind(a = rchisq(300, 2), b = rchisq(300, 2.2), c = rchisq(300, 3))
tpr_mcs(L)
}
}
