% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{tpr_var_backtest}
\alias{tpr_var_backtest}
\title{Value-at-Risk backtests}
\usage{
tpr_var_backtest(
  variance_forecast,
  returns,
  alpha = 0.01,
  dist = c("normal", "student"),
  df = 5
)
}
\arguments{
\item{variance_forecast}{Numeric vector of one-step variance
forecasts, strictly positive.}

\item{returns}{Realized returns for the same days.}

\item{alpha}{Tail probability, for example `0.01`.}

\item{dist}{Either `"normal"` or `"student"`.}

\item{df}{Degrees of freedom when `dist = "student"`. Default `5`.}
}
\value{
A one-row `data.table` with the violation rate and both
  tests.
}
\description{
Builds \eqn{\widehat{VaR}_{t+1}=z_\alpha\sqrt{\hat f_{t+1}}} from a
variance forecast and tests the resulting violation sequence with the
unconditional coverage test of Kupiec (1995) and the independence
test of Christoffersen (1998).
}
\examples{
set.seed(1)
v <- rep(4e-4, 1000); r <- rnorm(1000, sd = sqrt(v))
tpr_var_backtest(v, r, alpha = 0.05)
}
\references{
Kupiec, P. (1995). Techniques for verifying the accuracy of risk
measurement models. \emph{Journal of Derivatives} 3, 73-84.
Christoffersen, P. (1998). Evaluating interval forecasts.
\emph{International Economic Review} 39, 841-862.
}
