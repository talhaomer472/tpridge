% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/har.R
\name{har_features}
\alias{har_features}
\title{HAR features from a daily realized measure}
\usage{
har_features(rv, date = NULL, lags = c(1, 5, 22), log = TRUE, extra = NULL)
}
\arguments{
\item{rv}{Numeric vector of daily realized variance, in time order.}

\item{date}{Optional vector of dates, carried through to the output.}

\item{lags}{Averaging windows. Default `c(1, 5, 22)`, the daily,
weekly and monthly components of Corsi (2009).}

\item{log}{Return the logarithm of each component. Default `TRUE`.}

\item{extra}{Optional `data.frame` of further predictors, aligned
with `rv`, appended unchanged.}
}
\value{
A `data.table` whose first column `y` is the target for the
  next day, followed by the HAR components and any extras.
}
\description{
Builds the daily, weekly and monthly components of the HAR model as
rolling averages ending at \eqn{t-1}, together with the lead of the
target, so that each row contains a forecast origin and the value it
predicts. The logarithmic transformation is the default because a
squared error criterion applied to realized variance in levels is
dominated by a handful of days.
}
\examples{
set.seed(1)
rv <- har_simulate(500, sigma2 = 0.5)
d <- har_features(rv)
head(d)
}
\references{
Corsi, F. (2009). A simple approximate long-memory model of realized
volatility. \emph{Journal of Financial Econometrics} 7, 174-196.
}
