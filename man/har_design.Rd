% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/har.R
\name{har_design}
\alias{har_design}
\title{Design matrix and response from a feature table}
\usage{
har_design(features, predictors = NULL)
}
\arguments{
\item{features}{Output of [har_features()].}

\item{predictors}{Character vector of column names to use. Default is
every column other than `y` and `date`.}
}
\value{
A list with `X`, `y` and `date`, complete cases only.
}
\description{
Design matrix and response from a feature table
}
\examples{
set.seed(1)
d <- har_features(har_simulate(500, sigma2 = 0.5))
des <- har_design(d)
dim(des$X)
}
