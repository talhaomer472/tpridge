% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/har.R
\name{har_simulate}
\alias{har_simulate}
\title{Simulate a HAR process}
\usage{
har_simulate(n, sigma2, alpha = 0.1, betas = c(0.3, 0.3, 0.3), burn = 300L)
}
\arguments{
\item{n}{Number of usable observations returned.}

\item{sigma2}{Variance of the multiplicative shock, equal to
\eqn{1/k}.}

\item{alpha}{Intercept, strictly positive.}

\item{betas}{The three persistence parameters, non-negative and
summing to less than one.}

\item{burn}{Burn-in discarded before the returned sample.}
}
\value{
A numeric vector of length `n + 22`, so that
  [har_features()] yields exactly `n` usable rows.
}
\description{
Generates \eqn{RV_t=\mu_t\eta_t} with
\eqn{\mu_t=\alpha+\beta_d RV_{t-1}+\beta_w \overline{RV}^{(w)}_{t-1}
+\beta_m \overline{RV}^{(m)}_{t-1}} and
\eqn{\eta_t\sim\mathrm{Gamma}(k,1/k)} with unit mean. Positivity is
structural: no truncation or transformation is applied, and the
conditional mean is exactly the HAR conditional mean.
}
\examples{
set.seed(1)
rv <- har_simulate(1000, sigma2 = 0.5)
c(mean = mean(rv), min = min(rv))
}
