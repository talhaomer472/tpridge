% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montecarlo.R
\name{tpr_montecarlo}
\alias{tpr_montecarlo}
\title{Monte Carlo study of the three estimators}
\usage{
tpr_montecarlo(
  n = c(100, 200, 400, 500),
  sigma2 = c(0.1, 0.4, 0.5, 0.6, 0.7, 0.8, 1, 1.1, 1.2),
  reps = 1000L,
  train_frac = 0.4,
  rule = c("hkb", "df"),
  target_df = c(3, 2.5, 2, 1.5, 1, 0.5),
  alpha = 0.1,
  betas = c(0.3, 0.3, 0.3),
  seed = NULL,
  progress = TRUE
)
}
\arguments{
\item{n}{Sample sizes to consider.}

\item{sigma2}{Variances of the multiplicative shock.}

\item{reps}{Replications per cell.}

\item{train_frac}{Share of each sample used for estimation.}

\item{rule}{Penalty rule, `"hkb"` or `"df"`.}

\item{target_df}{Targets used when `rule = "df"`.}

\item{alpha, betas}{Parameters of the data-generating process.}

\item{seed}{Seed for reproducibility.}

\item{progress}{Report each cell as it completes.}
}
\value{
An object of class `tpr_mc`, a `data.table` with one row per
  replication, cell and estimator.
}
\description{
Reproduces the design of the accompanying paper. For each cell of the
grid the process of [har_simulate()] is drawn afresh, split into an
estimation and an evaluation part, and all three estimators are
rolled forward. When several penalty targets are supplied they are
applied to the same simulated path within a replication, so the
comparison across penalties is paired.
}
\examples{
\donttest{
mc <- tpr_montecarlo(n = 200, sigma2 = 0.5, reps = 20, seed = 1)
print(mc)
}
}
