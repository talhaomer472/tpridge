% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot.tpr_fit}
\alias{plot.tpr_fit}
\title{Plot a fitted model}
\usage{
\method{plot}{tpr_fit}(x, which = c("path", "coef", "gap"), n_grid = 40L, ...)
}
\arguments{
\item{x}{An object of class `tpr_fit`.}

\item{which}{One of `"path"`, the coefficient of determination of all
three estimators against the effective degrees of freedom;
`"coef"`, the standardized coefficients; or `"gap"`, the fit gain
of Proposition 2 against the penalty.}

\item{n_grid}{Number of penalty values on the path.}

\item{...}{Passed to the underlying plotting calls.}
}
\value{
Invisibly, the data behind the plot.
}
\description{
Plot a fitted model
}
\examples{
set.seed(1)
X <- matrix(rnorm(300), ncol = 3)
y <- as.numeric(X \%*\% c(1, 0.5, 0.2)) + rnorm(100)
plot(tpr_fit(X, y), which = "path")
}
